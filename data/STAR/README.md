# STAR Video Placeholder

Place STAR videos here if your evaluation JSON uses repository-relative paths:

```text
data/STAR/
```

The evaluation code reads the path from each sample's `video` field, so absolute paths or symlinks also work.

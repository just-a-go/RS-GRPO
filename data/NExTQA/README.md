# NExT-QA / NExT-GQA Video Placeholder

Place NExT-QA or NExT-GQA videos here if your evaluation JSON uses repository-relative paths:

```text
data/NExTQA/videos/
```

The evaluation code reads the path from each sample's `video` field, so absolute paths or symlinks also work.
